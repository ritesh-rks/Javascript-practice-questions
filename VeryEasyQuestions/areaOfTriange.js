// Write a function that takes the base and height of a triangle and return its area.

const areaOfTriangle = (base, height) => 1/2*(base * height);

const result = areaOfTriangle(3,2);
console.log(`Area of triangle  is ${result}`);