// Create a function that returns true when num1 is equal to num2; otherwise return false.

const isSameNumber = (num1, num2) => num1 === num2;

const result = isSameNumber(8,8);

console.log(result);